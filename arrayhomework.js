function esParOImpar(num) {
    if (num % 2 === 0) {
      console.log(`${num} es par`);
    } else {
      console.log(`${num} es impar`);
    }
  }
  
  esParOImpar(6); 
  esParOImpar(9); 

  const esParOImpar = (num) => {
    if (num % 2 === 0) {
      console.log(`${num} es par`);
    } else {
      console.log(`${num} es impar`);
    }
  }
  
  esParOImpar(6); 
  esParOImpar(9); 
  